set(CMAKE_INSTALL_PREFIX "$ENV{HOME}/opt/ariadne/cxx" CACHE PATH "Local CXX installation path")
set(PYTHON_BINDINGS_INSTALL_DIR "$ENV{HOME}/opt/ariadne/python" CACHE PATH "Local Python installation path")

set(CMAKE_PREFIX_PATH "$ENV{HOME}/opt/mpfr;$ENV{HOME}/opt/gmp" CACHE PATH "Search for locally installed MPFR and GMP librarires")

set(CMAKE_BUILD_TYPE "Release" CACHE STRING "Set configurnation type to release")

# To configure for a release build call:
# $ cmake -S . -B build/Release -G 'Ninja' -C CMakeCache.Release.txt
